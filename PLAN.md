# خطة الإصلاحات قبل النشر — «هامش»

> وثيقة داخلية ناتجة عن فحص شامل للمشروع بهدف جاهزيته للنشر.
> الفئة المستهدفة: **مستخدمو الهواتف المحمولة** أولًا.
> التاريخ: 21 آب 2026 — الحالة: فحص مكتمل، الإصلاحات لم تبدأ بعد.

---

## 1. نطاق الفحص

فُوِّضت 6 جهات فحص متوازية غطّت المشروع كاملًا من زوايا مختلفة، مع تدقيق شخصي مباشر للنتائج الحرجة (قراءة `app.js` و`index.html` و`styles.css` سطرًا سطرًا) قبل اعتمادها:

| # | الجهة | النطاق |
|---|---|---|
| 1 | نواة التشغيل | إدارة الحالة، عرض PDF، التنقل بين الصفحات، المحررات (`app.js`) |
| 2 | PWA والعمل دون اتصال | `sw.js`، `manifest.json`، ترويسة `index.html` |
| 3 | التعقيم والاستيراد/التصدير والأمان | `cleanHtml`، صيغة `.hamsh`، تصدير PDF/Markdown |
| 4 | مطابقة المتطلبات | PRD.md مقابل AGENTS.md مقابل التنفيذ الفعلي |
| 5 | واجهة الهاتف وإتاحة الوصول | `index.html`، `styles.css`، `favicon.svg` — الاستجابة، اللمس، التباين |
| 6 | تحقق شخصي مباشر | إعادة فحص كل النتائج الحرجة في الكود المصدري قبل اعتمادها |

---

## 2. فهم البنية

تطبيق صفحة واحدة بدون أدوات بناء:

- `index.html` يحمّل `app.js` كـ ES Module.
- `app.js` يدير حالة عالمية واحدة (`state`) ويستدعي PDF.js ديناميكيًا من cdnjs (`getPdfJs`).
- الملاحظات HTML تُلتقط من محررَي `contenteditable` وتُعقَّم عبر `cleanHtml` عند كل حفظ/فتح/تنقل.
- تُخزَّن داخل ملف `.hamsh`: JSON يحوي المستند الأصلي بترميز base64 + صفحات الهوامش + بيانات وصفية.
- `sw.js` يوفّر العمل دون اتصال: network-first لأغلفة التطبيق (`index.html`, `styles.css`, `app.js`)، cache-first لكل ما عداه مع تخزين ديناميكي لخطوط Google.
- `manifest.json` يتيح التثبيت كتطبيق مستقل.
- مسار تصدير PDF: رسم كل صفحة على canvas مركّب (صفحة + هامش جانبي + هامش سفلي) عبر pdf.js بمقياس ×2، ثم بناء مستند جديد عبر pdf-lib وتضمين الصور PNG.

---

## 3. النتائج

### 🔴 حرجة / عالية

#### N1 — عنوان المستند غير مرئي تمامًا في الترويسة بالوضع الفاتح
- **الخطورة:** حرجة — أول ما يراه مستخدم الهاتف بعد فتح أي مذكرة.
- **الموقع:** `styles.css:11-12` (الرمزان)، `styles.css:125` (الترويسة)، `styles.css:127` (اسم المستند).
- **الدليل:**
  ```css
  :root { --text: var(--ink); --header-bg: var(--ink); }   /* كلاهما #12333a */
  .workspace-header { color: var(--text); background: var(--header-bg); }
  ```
- **مسار التنفيذ:** فتح أي PDF ← إظهار `#workspace-view` ← `<span id="document-title">` يرث `color:#12333a` فوق خلفية `#12333a` — نسبة تباين **1.0:1** (نصٌّ فوق نفس اللون). زر الرجوع `×` بلون `--muted` على الخلفية ذاتها ≈ 2:1 (فاشل أيضًا).
- **السبب الجذري:** رمزا `--text`/`--header-bg` صُمما للوضع الداكن وأُعيد استخدامها في `:root` الفاتح دون تعريف مناسب.
- **التأثير:** اسم المستند مخفي بالكامل في الوضع الفاتح (الافتراضي) على كل الأجهزة. الوضع الداكن سليم لأنه يعيد تعريف الرمزين (`styles.css:20-27`).
- **الإصلاح:** في `:root`: تعريف `--text: #fffef9` (أو أبيض دافئ من اللوحة) مع إبقاء قيم الوضع الداكن كما هي. سطران.
- **الحالة:** ✅ مؤكد شخصيًا من الكود.

#### N2 — `flattenFontSizeSpans` يعيد ترتيب الكلمات ويلغي حجم الخط الأحدث — تلفٌ دائم للملاحظات
- **الخطورة:** حرجة — فقدان بيانات صامت ويُخزَّن في الملف المحفوظ.
- **الموقع:** `app.js:96-108`.
- **الدليل:**
  ```js
  while (span.firstChild) current.appendChild(span.firstChild);
  span.remove();
  ```
  `appendChild` ينقل العقد إلى **نهاية** الحاوية الخارجية، فيكسر ترتيب المحتوى إن كان بعد المتداخل إخوة: `"hello world again"` ← `"hello againworld"`؛ وعند ≥3 مستويات تعشيش يتشوّه الترتيب بشكل غير متوقّع.
  كما أن الكود يُبقي حجم **الخارجي** بينما التعليق (سطر 96) يعد بالإبقاء على **الأعمق** — من صغّر كلمة داخل نص أكبر يفقده اختياره بعد أول تنقل/حفظ.
- **مسار التنفيذ:** تنسيق كلمة بحجم خط (execCommand ينتج spans متداخلة) ← أي التقاط/فتح يستدعي `cleanHtml` ← إعادة ترتيب مرئية عبر `fillEditors` ← **تُحفَظ النتيجة المشوّهة في كل ملف `.hamsh`**.
- **الإصلاح:** إعادة كتابة الدمج بالإدراج في الموضع (`before(span, …)` بدل الإلحاق بالنهاية)، وحسم الاتجاه الصحيح (الأعمق = آخر تنسيق طبّقه المستخدم) ليطابق التعليق.
- **الحالة:** ✅ مؤكد شخصيًا من الكود.

#### N3 — سباق التنقل السريع: نقرات متتالية تخلط ملاحظات الصفحات
- **الخطورة:** عالية — تلف بيانات، والفئة المستهدفة تُنقر بسرعة على الهاتف.
- **الموقع:** `renderPage` (`app.js:141-152`) مع معالجات التنقل (`app.js:502-503, 524-530`).
- **الدليل:** لا يوجد أي قفل أو علم «تنفيذ جارٍ» حول `renderPage` غير المتزامنة.
- **مسار التنفيذ:** نقرة «التالي» تبدأ `renderPage()` ← نقرة ثانية قبل انتهائها ← `captureEditors()` في النقرة الثانية تقرأ `state.pages[state.activePage - 1]` **بعد** أن غيّرت النقرة الأولى `activePage` بينما المحررات ما زالت تعرض محتوى الصفحة السابقة ← كتابة ملاحظات صفحة N في خانة N+1 (والعكس).
- **الإصلاح:** رمز تنقّل (token) أو تعطيل أزرار التنقل حتى يستقر `renderPage`، مع ضمان التقاط المحتوى **قبل** أي تغيير لـ `activePage`. اختبار: إطلاق نقرتين برمجيتين متتاليتين والتأكد أن `pages[1].sideMargin === ''`.

#### N4 — عيبان في `sw.js` يهددان العمل دون اتصال والبيانات
1. **فشل تثبيت SW عند ضعف الشبكة (addAll الذرية):**
   - **الموقع:** `sw.js:19` مع القائمة في `sw.js:2-12`.
   - `cache.addAll()` ذرّية، والقائمة تضم رابطَي cdnjs الخارجيين (~1MB للـ worker). فشل/بطء طلب واحد على شبكة هاتف ضعيفة ← رفض الوعد بالكامل ← **لا SW إطلاقًا** ← لا عمل دون اتصال ولا تجربة تثبيت جيدة.
   - **الإصلاح:** تخزين cdnjs ديناميكيًا (فرع cache-first موجود أصلًا في `sw.js:65-83`) بدل التخزين المسبق الذري، أو `Promise.allSettled` بـ `cache.add()` مفردة.
2. **إعادة تحميل قسرية تمحو ملاحظات غير محفوظة:**
   - **الموقع:** `app.js:541-545` + `sw.js:20,36`.
   - عند نشر نسخة جديدة: `skipWaiting → clients.claim → controllerchange → location.reload()` بلا أي فحص للعمل غير المحفوظ. مستخدم عاد لمواصلة الكتابة بعد تحديث الموقع يخسر كل ما كتبه منذ آخر حفظ صريح.
   - **الإصلاح:** تأجيل إعادة التحميل حتى تكون مساحة العمل فارغة/محفوظة (استراتيجية network-first للأغلفة تضمن وصول الكود الجديد عند الزيارة التالية على أي حال).

#### N5 — شريط التنسيق معطّل على iOS وغير قابل للوصول بلوحة المفاتيح
- **الخطورة:** عالية — وظيفة أساسية للفئة المستهدفة.
- **الموقع:** `app.js:505-511` (معالجات `mousedown` فقط مع `preventDefault`). لا وجود لأي `pointerdown/touchstart` في المشروع.
- **المشكلة المزدوجة:**
  1. **لوحة المفاتيح:** Enter/Space يطلقان حدث `click` لا `mousedown` — الأزرار تصل إليها علامة Tab لكن الضغط عليها **لا يفعل شيئًا**. إخفاق مباشر لبند DoD الخاص بإتاحة لوحة المفاتيح.
  2. **iOS Safari/PWA:** انهيار التحديد يقع خلال مرحلة اللمس (`touchstart/touchend`) **قبل** إطلاق mousedown، فيطبّق التنسيق على لا شيء أو على موضع خاطئ — بصمت ودون رسالة خطأ. على أندرويد يعمل غالبًا لكنه ينكسر عند فقدان التركيز (بعد تمرير أو إغلاق لوحة المفاتيح).
- **الإصلاح:** حفظ التحديد (`Range` + العنصر النشِط) عند `pointerdown`، واستعادة التركيز والتحديد قبل `execCommand`، مع فرع `keydown` (Enter/Space) للوحة المفاتيح. اختبار على جهازَي iOS وأندرويد.

#### N6 — ترويسة مساحة العمل وشريط التنسيق يفيضان عن شاشات 320–430px
- **الخطورة:** عالية — معظم عرض هواتف أندرويد.
- **الموقع:** `styles.css:125,147,154` + `index.html:52-68` (الترويسة)؛ `styles.css:137,154` + `index.html:92-105` (شريط التنسيق).
- **الدليل:** `.workspace-header { display:flex; gap:22px }` بلا `flex-wrap` (≤900px يقلل الفراغ إلى 10px فقط). الميزانية المحسوبة للترويسة: رجوع 32 + مجموعة المقاس ~100 + تصدير PDF ~90 + تصدير .md ~85 + حفظ ~65 + تبديل سمة 38 + فراغات ≈ **455px** مقابل 320–390px متاحة. شريط التنسيق ≈ **380px** مقابل 320–360px بنفس المشكلة.
- **التأثير:** أزرار الحفظ/التصدير مقصوصة أو خارج الشاشة، أو تمرير أفقي للصفحة.
- **الإصلاح:** `flex-wrap: wrap` للعنصرين + إعادة ترتيب/تكثيف العناصر على الشاشات الضيقة (أيقونات بدل نصوص حيث يلزم مع الحفاظ على `aria-label`).

---

### 🟠 متوسطة

#### M1 — أهداف لمس أصغر من 44px بشكل نظامي
- **الموقع:** `styles.css:126-137`.
- أزرار التنقل 35×35، أزرار التنسيق 29px ارتفاعًا، تبديل السمة 38×38، زر الرجوع 32×32، والمقياس ×1..×4 بفاصل **1px** في ≤500px (`styles.css:128,154`) — أسوأ الحالات. احتمال نقر خاطئ هائل في كل عمليات التنقل والحفظ والتنسيق.
- **الإصلاح:** رفع الحد الأدنى إلى ~44×44px وفاصل ≥8px، خاصة مجموعة المقاس وشريط التنسيق.

#### M2 — لا آلية تكبير للصفحة + ضبابية DPR
- **الموقع:** `app.js:145-148`, `styles.css:133`.
- الرسم بثابت `getViewport({scale: 1.45})` دون مضاعفة `devicePixelRatio`؛ على شاشة 3x يكون الخرج أنعم قليلًا من حدّة الجهاز، ونص A4 المعروض بعرض ~330px CSS غير مقروء إلا بتكبير النظام الذي يكبّر الواجهة كلها وليس PDF فقط. لا توجد آلية تكبير داخلية (لا أزرار ولا إيماءات خاصة)، ولا `touch-action` يقيد الإيماءات.
- **الإصلاح (على مرحلتين):** (1) مضاعفة المقياس بـ `Math.min(devicePixelRatio, 2–3)` في `renderPage` مع إبقاء العرض عبر CSS كما هو؛ (2) لاحقًا: آلية تكبير داخلية (قرص أو أزرار) تعيد الرسم بالمستوى الجديد.

#### M3 — إخفاقات تباين WCAG
- **الموقع:** `styles.css:64` (تسميات مرجانية)، `135` (placeholder)، `136` (خطأ)، `34` (حلقة تركيز).
- تسميات الهوامش المرجانية 11px على الورق = **2.99:1** (مطلوب 4.5 للنص الصغير) — وهي المؤشر المكاني الأساسي للمنتج؛ placeholder = **2.19:1**؛ نص الخطأ = **3.72:1**؛ حلقة التركيز = **2.16:1** (المطلوب 3:1 لعناصر الواجهة). في المقابل: أزواج النصوص الأساسية (ink/paper 13.3، ink/desk 11.8، أبيض/ink 13.5، نص المحرر 11.4) تاجوز AAA.
- **الإصلاح:** تعتيم المرجاني لتسميات الهوامش أو تكبيرها/تغميقها، تغميق placeholder وحلقة التركيز ونص الخطأ.

#### M4 — الأزرار الأساسية «تذوب» في ترويسة الوضع الداكن
- **الموقع:** `styles.css:70-71` مع `24`.
- خلفية `.button-save`/`.button-primary` تبقى `--ink` (#12333a) بلا حدود فوق `--header-bg` الداكن (#071d22) — نسبة سطح-على-سطح **1.29:1**. حفظ/تصدير PDF/تصدير .md بلا حواف مرئية تقريبًا (النص الأبيض الداخلي مقروء فقط).
- **الإصلاح:** حدود صريحة أو خلفية أفتح لهذه الأزرار في `[data-theme="dark"]`.

#### M5 — فشل عرض صفحة يترك activePage متقدمًا
- **الموقع:** `app.js:150` مع `502-503, 524-530`.
- إذا فشل `getPage/render` (انهيار worker، ذاكرة منخفضة، صفحة تالفة): `activePage` زاد **قبل** الانتظار، ومسار catch لا يتراجع عنه ولا يستدعي `fillEditors/updateControls`. الواجهة تعرض صفحة N والحالة تقول N+1 ← النقرة التالية تكتب ملاحظات الصفحة المعروضة في الخانة الخطأ حتى بسرعة نقر عادية.
- **الإصلاح:** حفظ القيمة السابقة لـ `activePage` في كل معالج؛ عند الفشل استرجاعها وإعادة `fillEditors()/updateControls()` (أو نقل `activePage++` إلى ما بعد نجاح `getPage`).

#### M6 — exportPdf يتسابق مع الواجهة الحية
- **الموقع:** `app.js:300-302, 477-484`.
- `setHeaderDisabled` يعطّل 3 أزرار فقط؛ زر الرجوع والأزرار الرئيسية والتنقل والتنسيق والفواصل تبقى حية خلال حلقة التصدير غير المتزامنة التي تعيد قراءة `state.pageCount/pages/pdfDocument` كل تكرار. فتح ملف آخر أثناء التصدير يمزج صفحات مستندين في مخرج واحد **صامت**، أو يرمي خطأً مضللًا بعد تدمير المستند الجاري تصديره. محتمل جدًا على الهاتف حيث تستغرق عملية التصدير ثوانٍ إلى دقائق.
- **الإصلاح:** تعطيل كامل التفاعلات (أو طبقة حجب) طوال التصدير، وربط الحلقة بلقطة ثابتة من البيانات.

#### M7 — openHamsh يثبّت الحالة قبل اكتمال التحقق
- **الموقع:** `app.js:190-199`.
- `loadPdf/state.pdfBytes/state.pages/marginScale` تُستبدل قبل خطوات قابلة للفشل. ملف `.hamsh` عدائي بقيمة `meta` أولية (`"x"`/رقم) يجعل السطر 196 يرمي `TypeError: Cannot create property 'title' on string 'x'` إنجليزيًا خامًا في toast، ويترك الحالة العالمية مشيرة للمستند المرفوض. مخالف لمعيار قبول PRD §11 («يُرفض الملف غير الصالح دون انهيار الواجهة») وقاعدة النسخ العربي.
- **الإصلاح:** التحقق من شكل `meta` ضمن `validateHamsh`؛ تجهيز النتائج في متغيرات محلية واعتمادها في `state` فقط بعد نجاح جميع الخطوات؛ رسائل خطأ عربية للمستخدم.

#### M8 — URL شخصي مضمّن في كل تصدير Markdown بصيغة مكسورة
- **الموقع:** `app.js:267`.
- `lines.push('#هامش ¦ abonjm970.github.io/hamesh')` — لا مسافة بعد `#` فلا يُعرض heading بل نص حرفي، وفي نهاية ملف كل مستخدم توقيع بعنوان GitHub Pages الشخصي للمطوّر. غير مهني للنشر العام.
- **الإصلاح:** حذف السطر أو جعله توقيعًا اختياريًا بصيغة صحيحة.

#### M9 — أيقونة iOS = لقطة شاشة، وmanifest يعتمد SVG فقط
- **الموقع:** `index.html:8` + `manifest.json:13-26`.
- `apple-touch-icon` يشير إلى SVG ولا يدعمه iOS ← «إضافة إلى الشاشة الرئيسية» تنتج أيقونة لقطة شاشة مشوهة. manifest بلا PNG حقيقية: Chrome الحديث يقبل SVG `"any"` للتثبيت، لكن Lighthouse/PWABuilder ترفضها وبعض قوائم OEM وWebView القديمة تهملها. فنّ maskable نفسه آمن ضمن المنطقة الآمنة.
- **الإصلاح:** توليد PNG 192/512 (+maskable) وربط `apple-touch-icon` بـ PNG 180.

---

### 🟡 منخفضة

#### L1 — أول تصدير PDF يمحو نص مؤشر التحميل نهائيًا
- **الموقع:** `app.js:295-299, 495` + `index.html:78`.
- `setLoadingMessage('')` في `finally` يدمر عقدة النص الأصلية «يُحضّر الصفحات…» بلا مصدر استرجاع؛ كل مؤشرات التحميل اللاحقة في الجلسة بلا نص.
- **الإصلاح:** استرجاع النص الأصلي بدل الفراغ.

#### L2 — مسافة بادئة معكوسة للقوائم في PDF المصدَّر
- **الموقع:** `app.js:343-355` مقابل `379-384`.
- التخطيط يحجز `indent=20` عند لفّ **كل** أسطر عنصر القائمة، لكن الرسم يسحب المسافة البادئة من سطر الأول فقط (حيث `line.marker`) ← أسطر الالتفاف تبدأ 20px يمين بداية سطر الأول (مسافة بادئة معكوسة) في كل عنصر قائمة متعدد الأسطر. لا قص/تجاوز — خلل طباعي فقط.
- **الإصلاح:** تطبيق `line.indent` على كل الأسطر في `drawNoteLines`.

#### L3 — القوائم المتداخلة تلتحم في تصدير Markdown
- **الموقع:** `app.js:216-233`.
- `<ol><li>أول<ol><li>فرعي</li></ol></li>…` يُصدَّر `"1. أول1. فرعي"` — الهرمية والترقيم تنهاران نصًّا ملتحمًا؛ `<strong>` الفارغ ينتج `****`.
- **الإصلاح:** معالجة القوائم المتداخلة بمسافات بادئة وكسر أسطر صريح.

#### L4 — استنفاد كومة التعقيم بتعشيش عدائي عميق
- **الموقع:** `app.js:81-87`.
- ~5000 وسم متداخل من ملف `.hamsh` عدائي يرمي `RangeError` (تم التحقق تنفيذيًا) — محصور برسالة إنجليزية مربكة ورفض فتح الملف؛ لا حلقة لانهائية ولا فقد عقد.
- **الإصلاح (اختياري):** تحويل التكرار العميق إلى تكرار صريح بمكدس، أو حد أقصى للعمق في `validateHamsh`.

#### L5 — خطأ مطبعي في `<title>`
- **الموقع:** `index.html:14` — «هامش — مدير مذكرات **مذكرات**» (كلمة مكررة). يظهر في التبويبات وسجل التنقل وأوراق المشاركة وعناوين نافذة PWA.
- **الإصلاح:** استعادة العنوان الأصلي «هامش — اقرأ واكتب بجانب الصفحة».

#### L6 — divider-select بخط 12px يطلق تكبير iOS التلقائي
- **الموقع:** `styles.css:137`. مدخلات/عناصر تحكم بخط <16px تسبب تكبير الصفحة تلقائيًا عند التركيز في iOS Safari. يظهر فقط عند وجود فواصل.
- **الإصلاح:** رفع حجم خط العنصر إلى ≥16px.

#### L7 — NETWORK_FIRST يطابق باللاحقة عبر كل النطاقات
- **الموقع:** `sw.js:41,48`. `url.pathname.endsWith('/app.js' …)` بلا فحص origin — يوجَّه `/app.js` أجنبي إلى network-first ويُخزَّن في كاش التطبيق عند نجاح 200 (تلويث حصة التخزين). غير مؤذٍ وظيفيًا اليوم لأن التطبيق لا يحمّل JS/CSS خارجيًا آخر.
- **الإصلاح:** إضافة شرط `url.origin === self.location.origin`.

#### L8 — قيمة غير صالحة لشريط حالة iOS
- **الموقع:** `index.html:13` — `apple-mobile-web-app-status-bar-style` بقيمة `#12333a`؛ المسموح `default|black|black-translucent`. يتجاهلها iOS.
- **الإصلاح:** `black-translucent` أو الحذف.

#### L9 — توثيق منحرف عن الواقع
- PRD.md **محذوف من المستودع** رغم أن AGENTS.md يجعله المرجع الإلزامي، بينما تغيّر مخطط `.hamsh` فعليًا (حقل `divider`، `viewState.activePage`، `originalFileName`) وما زال `formatVersion: "1.0"`.
- README.md يشير إلى سير عمل Pages (`.github/workflows/pages.yml`) محذوف، ويسرد PRD.md في جدول البنية، ويعتمد خطّي Google Fonts بينما المحمَّل ثلاثة مع pdf-lib مضاف، وتغيب عن قائمة ميزاته: الوضع الداكن، الفواصل، تصدير PDF، تصدير Markdown، PWA/دون اتصال. سطر زائد `# hamesh` في النهاية، وبلا ترخيص رغم نصحه بالنشر العام.
- **الإصلاح:** استعادة PRD.md وتعديله ليطابق المخطط الفعلي، وتحديث README (تعليمات النشر الفعلية، الميزات، الأصول).

#### L10 — min-height:100vh مقابل أشرطة أدوات الجوال الديناميكية
- **الموقع:** `styles.css:54,124`. على iOS تتجاوز 100vh المساحة المرئية فتظهر آخر ~60px (غالبًا شريط التنسيق) تحت شريط العنوان حتى تمرير يدوي. لا `dvh/svh` ولا `interactive-widget`.
- **الإصلاح:** `min-height: 100dvh` مع بديل vh.

#### L11 — غياب العناوين في مساحة العمل
- العنوان الوحيد h1 في الصفحة الرئيسية (`index.html:27`)؛ عناوين اللوحات `<div>/<span>` (`index.html:85,89`). مستخدمو قارئ الشاشة بلا بنية تنقل داخل مساحة العمل (الأقسام مسماة عبر `aria-labelledby` فالأثر محدود).
- **الإصلاح:** تحويل عناوين اللوحات إلى `h2` مخفية بصريًا أو ظاهرة.

#### L12 — عدم تماثل أزرار المقياس مع التحقق (أثر PRD)
- الواجهة تعرض ×1,×2,×4 (`index.html:59-61`) بينما `validateHamsh` يقبل {1,2,3,4} وPRD (حسب الفحص) نص على {1,2,3} — زر ×3 حُذف من الواجهة بصمت. انحراف توثيقي/تنفيذي يجب حسمه وتوثيقه.
- **الإصلاح:** قرار منتج واحد: إما إرجاع ×3 أو توحيد القبول والتوثيق على {1,2,4}.

---

## 4. ملاحظات غير محسومة (تحتاج اختبارًا يدويًا)

- `beforeunload` غالبًا يُتجاهل في PWA iOS المستقلة — حماية العمل غير المحفوظ قد لا تظهر إطلاقًا هناك عند الإغلاق من مبدّل التطبيقات.
- حد iOS لذاكرة canvas (~16.7M بكسل) قد يُفرغ صفحات ضخمة في تصدير PDF — محسوب نظريًا (A4 آمن؛ صفحات أكبر بكثير من A1 تتجاوزه)، يحتاج اختبار جهاز.
- ذروة الذاكرة أثناء الحفظ/التصدير ≈ 4–5× حجم PDF بسبب base64 — ملفات كبيرة على هواتف ضعيفة قد تفشل.
- افتراض تخزين خطوط Google ديناميكيًا يعتمد على وضع CORS لطلبات `<link>` — يستحق تأكيدًا واحدًا في DevTools (Application → Cache Storage).
- تشكيل RTL في canvas عند نص مختلط (أرقام/لاتيني داخل العربية) في تصدير PDF — الكلمة تُرسم يمين→يسار بالترتيب المنطقي، صحيح للعربية الخالصة، مشكوك فيه للتسلسلات المدمجة. يحتاج اختبار ملاحظة واقعية مختلطة.
- قابلية استخدام الوضع الأفقي: الهوامش تحت الطية بأي اتجاه تخطيط — قرار تصميمي أكثر منه خللًا.
- تكديس الهامش الجانبي أسفل الصفحة ≤900px: هل هو مقبول تصميميًا أم مخالف لقاعدة AGENTS.md («الهامش الجانبي يمين الـ PDF في كل الأوقات»)? قرار منتج.
- تجربة قارئات الشاشة فعليًا (VoiceOver/NVDA) — التسميات موجودة (25 aria-label) لكن التجربة الكاملة تحتاج جهازًا.

---

## 5. ما ثبت صحته (تغطية الفحص)

- `bytes.slice(0)` في `loadPdf` يحمي `state.pdfBytes` من فصل المخزن بواسطة pdf.js — ضروري وصحيح.
- قائمة تعقيم `cleanHtml` البيضاء تطابق AGENTS.md تمامًا (`p, br, strong, b, ul, ol, li, span[data-font-size]`) وبلا ثغرة XSS قابلة للتتبع (DOMParser ثم innerHTML لمحتوى معقّم؛ إزالة كل السمات عدا `data-font-size∈{2,3,4}` على span فقط).
- تحويل DIV→P يمنع انهيار الأسطر بين الصفحات؛ خريطة FONT→span تغطي كل أحجام execCommand 1–7.
- `validateHamsh` صارم: الإصدار، مجال marginScale، تطابق pageCount مع طول pages، تسلسل pageNumber === i+1، تقييد viewState.activePage.
- لا localStorage/sessionStorage/IndexedDB في المستودع إطلاقًا — الامتثال الكامل لقاعدة الحفظ اليدوي.
- viewport meta يسمح بتكبير المستخدم (لا user-scalable=no) ولا touch-action يقيد الإيماءات.
- كل الأزرار الأيقونية لها `aria-label` عربي؛ دورا alert/status موجودان؛ `lang="ar" dir="rtl"` صحيحان؛ عزل `direction:ltr` لمكتب القراءة وrtl للوحات مطبق.
- `:focus-visible` مصمم للأزرار والمحررات؛ `prefers-reduced-motion` مُطبَّق عالميًا (`styles.css:156`).
- أحجام خطوط المحررات 19–30px ≥16px فلا تكبير iOS تلقائي عند التركيز فيها (الاستثناء: L6).
- الوضع الداكن يحافظ على ورق/محررات/مساطر بيضاء كما صُمم («ورق مضيء على مكتب داكن»).
- الصفحة الرئيسية تتكيف جيدًا ≤500px (تكديس specimen أولًا، أزرار بعرض كامل، h1 بـ clamp).
- لا تمرير أفقي لمكتب القراءة عند 320/360/390 عموديًا (canvas سائل، minmax(0,1fr)).
- تغطية أصول العمل دون اتصال كاملة: كل مورد يُطلب وقت التشغيل إما مسبق التخزين أو مخزَّن ديناميكيًا (الهشاشة الوحيدة هي ذرّية التثبيت N4-1).
- مفتاح fallback للتنقل في sw (`caches.match('./index.html')`) يُحل صحيحًا نسبةً إلى موقع السكربت.
- استراتيجية الإصدارات متماسكة: أغلفة network-first تجعل الأسماء غير المبعثرة آمنة؛ activate يمحو الكاش القديم؛ علم reloading يمنع حلقات إعادة التحمل.
- `bytesToBase64` بتقطيع 0x8000 آمن ضد تجاوز كومة الاستدعاء للملفات الكبيرة.
- سياق canvas المعاد استخدامه سليم بعد تصفير width/height.
- hover styles تجميلية فقط — لا وظيفة محجوبة عن اللمس.
- favicon.svg صالح ومتوافق مع اللوحة ومستقل عن الدقة.

---

## 6. خطة الإصلاحات

### الدفعة الحرجة — قبل النشر مباشرة
| الترتيب | النتيجة | الجهد | الاختبار |
|---|---|---|---|
| 1 | N1 تلاشي عنوان الترويسة | سطران في `:root` | فتح مذكرة بالوضع الفاتح ← ظهور اسم المستند |
| 2 | N6+M1 فيض الترويسة/التنسيق + أهداف اللمس | CSS | شاشة 360px ← لا فائض، أهداف ≥44px، فاصل ≥8px للمقياس |
| 3 | N2 تلف flattenFontSizeSpans | إعادة كتابة الدمج بالإدراج الموضعي + اعتماد الأعمق | كلمة مصغرة داخل نص أكبر تبقى كذلك بعد تنقل وحفظ/فتح؛ ترتيب الكلمات ثابت |
| 4 | N3+M5 سباق التنقل + التراجع عند الفشل | token قفل + rollback | نقرتان سريعتان ← `pages[1].sideMargin === ''`؛ محاكاة فشل عرض ← activePage يسترجع |
| 5 | N5 شريط التنسيق (لمس + لوحة مفاتيح) | pointerdown مع حفظ/استعادة Range + فرع keydown | iOS: غامق كلمة محددة ← ينجح؛ Tab+Enter ← ينجح |
| 6 | M8+L5 تنظيف التصدير والعنوان | حذف سطر URL، تصحيح title | تصدير md نظيف؛ العنوان الصحيح في التبويب |
| 7 | N4 عيبو sw.js | إخراج cdnjs من addAll + تأجيل reload | تثبيت بشبكة مقيدة ينجح؛ تحديث SW مع عمل غير محفوظ لا يمحوه |

### الدفعات التالية — بعد النشر الأول
- **M2**: مضاعفة DPR في renderPage ثم آلية تكبير داخلية.
- **M3+M4**: تباين التسميات/placeholder/التركيز/الخطأ + حدود الأزرار داكنًا.
- **M6+M7**: حجب كامل أثناء التصدير + اعتماد الحالة بعد اكتمال التحقق ورسائل عربية.
- **M9**: PNG 192/512 + apple-touch-icon PNG 180 + تصحيح status-bar-style (L8).
- **L9**: استعادة PRD.md معدلًا على المخطط الفعلي (divider, viewState, formatVersion) + تحديث README.
- **L1–L7, L10–L12**: فرادي حسب الأولوية.

### بوابة النهاية (Definition of Done) — اختبار يدوي نهائي
1. `node --check app.js`.
2. إنشاء مذكرة من PDF متعدد الصفحات على جهاز Android حقيقي وiPhone حقيقي.
3. كتابة في الهامشين بتنسيقات وقوائم وأحجام خط مختلفة، وتنقل سريع عمدًا بين الصفحات.
4. تدوير الجهاز (عمودي/أفقي)، تغيير المقياس، حفظ وإعادة فتح، فحص Console بلا أخطاء.
5. ملف `.hamsh` عدائي (meta أولية، تعشيش عميق، حقول ناقصة) ← رفض بأثر صفري على الحالة.
6. وضع الطيران بعد زيارة ناجحة واحدة ← التطبيق يعمل كاملًا دون اتصال.
7. تمرير VoiceOver/TalkBack على المسارات الأساسية.
