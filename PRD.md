# وثيقة متطلبات المنتج (PRD)

# هامش — Hamesh

**الإصدار المستهدف:** MVP (v1.0)  
**نوع المنتج:** تطبيق ويب يعمل محليًا بالكامل داخل المتصفح  
**حالة الوثيقة:** جاهزة للتنفيذ

## 1. الملخص التنفيذي

«هامش» أداة ويب تمكّن الطالب أو الباحث من قراءة ملف PDF وإضافة ملاحظات مرتبطة بكل صفحة في مساحتين منفصلتين: هامش جانبي ثابت إلى يمين الصفحة وهامش سفلي. لا يُعدّل التطبيق ملف PDF الأصلي ولا يرفع أي بيانات إلى خادم؛ بل يحفظ المستند الأصلي والملاحظات وإعدادات العرض معًا في ملف واحد قابل للنقل بامتداد `.hamsh`.

يركّز الإصدار الأول على تجربة قراءة وكتابة بسيطة، وملف حفظ موثوق يمكن فتحه لاحقًا على جهاز آخر يدعم التطبيق.

## 2. المشكلة والفرصة

تدوين الملاحظات على ملفات PDF غالبًا يخلط بين التعليق داخل المستند وتدوين الأفكار حوله، أو يتطلب حسابًا وخدمة سحابية. يحتاج المستخدم إلى مساحة كتابة واضحة مرتبطة بكل صفحة، من دون تغيير الملف الأصلي ومن دون فقدان قابلية نقل عمله كملف واحد مستقل.

## 3. الأهداف

### أهداف الإصدار الأول

- تمكين إنشاء مذكرة من أي ملف PDF محلي.
- ربط ملاحظات جانبية وسفلية مستقلة بكل صفحة.
- توفير الحد الأدنى المفيد من تنسيق النص: الغامق والقوائم النقطية والمرقمة.
- تصدير حالة المذكرة كاملةً إلى ملف `.hamsh` وإعادة فتحها لاحقًا.
- ضمان بقاء الملف والملاحظات محليًا في المتصفح وعلى جهاز المستخدم فقط.

### مؤشرات نجاح أولية

- يستطيع المستخدم إنشاء مذكرة، إضافة ملاحظات لصفحات متعددة، ثم إعادة فتح الملف المُصدّر مع استعادة المحتوى وإعداداته كما حُفظت.
- لا يحدث أي اتصال شبكي بغرض رفع ملفات المستخدم أو مزامنتها.
- يستطيع المستخدم الوصول إلى وظائف الإنشاء والفتح والحفظ والتنقّل من لوحة المفاتيح أو قارئ الشاشة بصورة معقولة.

## 4. المستخدمون المستهدفون

**الطلاب والباحثون** الذين يقرؤون ملفات PDF ويحتاجون إلى تلخيص الحجج أو الأسئلة أو المراجع لكل صفحة، ويرغبون في الاحتفاظ بمذكرة قابلة للنقل والمشاركة كملف واحد، من دون إنشاء حساب.

## 5. النطاق

### داخل النطاق

- رفع ملف PDF وإنشاء مذكرة جديدة منه.
- فتح ملف `.hamsh` متوافق.
- عرض صفحة PDF واحدة في كل مرة.
- هامش جانبي أيمن وهامش سفلي لكل صفحة.
- تحرير النص وتنسيقه بالغامق والقوائم النقطية والمرقمة.
- تغيير حجم الهوامش على مستوى المذكرة كاملةً (×1، ×2، ×3).
- حفظ يدوي وتنزيل ملف `.hamsh`.

### خارج النطاق

- الرسم الحر أو التعليقات فوق محتوى PDF.
- تعديل صفحات PDF أو تصدير PDF مشروح.
- الحفظ التلقائي أو استعادة المسودة من `localStorage` أو `IndexedDB`.
- حسابات المستخدمين أو المزامنة السحابية أو التعاون.
- سجل للمذكرات أو مكتبة للملفات المفتوحة سابقًا.
- البحث داخل PDF، التدوير، الطباعة، واستخراج النص.
- تنسيقات متقدمة مثل الألوان والخطوط والأحجام المخصصة والجداول والروابط.

## 6. المتطلبات الوظيفية

### 6.1 الصفحة الرئيسية

تعرض الصفحة خيارين واضحين:

1. **مذكرة جديدة:** يفتح منتقي الملفات، ويقبل ملفات PDF فقط.
2. **فتح مذكرة:** يفتح منتقي الملفات، ويقبل ملفات `.hamsh` فقط.

عند اختيار ملف غير صالح أو تعذّر قراءته، يعرض التطبيق رسالة خطأ مفهومة مع بقاء المستخدم في الصفحة الحالية.

### 6.2 إنشاء مذكرة جديدة

- يقرأ التطبيق ملف PDF محليًا ويتحقق من إمكان عرضه عبر PDF.js.
- ينشئ حالة مذكرة جديدة تتضمن عدد الصفحات واسم الملف الأصلي وتاريخ الإنشاء.
- يفتح مساحة العمل على الصفحة الأولى، بهامشين فارغين وبمقياس حجم `1`.
- لا يُعدّ إنشاء المذكرة حفظًا دائمًا؛ يلزم ضغط زر «حفظ» لتنزيل الملف.

### 6.3 فتح مذكرة موجودة

- يقرأ التطبيق ملف `.hamsh` ويتحقق من بنيته وإصدار صيغة الملف قبل فتحه.
- يستعيد ملف PDF المضمّن والملاحظات ومقياس الهوامش وآخر صفحة نشطة حُفظت.
- إذا كان الإصدار غير مدعوم أو كان الملف معطوبًا، لا يفتح التطبيق مساحة العمل ويعرض سببًا واضحًا للمستخدم.

### 6.4 مساحة العمل

- تعرض الشاشة صفحة PDF واحدة فقط في كل مرة، مع رقم الصفحة الحالي وإجمالي الصفحات.
- تتوفر أزرار «السابق» و«التالي»؛ يُعطّل «السابق» في الصفحة الأولى و«التالي» في الصفحة الأخيرة.
- عند الانتقال بين الصفحات، تُحفظ تعديلات الصفحة الحالية في ذاكرة التطبيق قبل العرض الجديد.
- لكل صفحة محرران مستقلان:
  - **الهامش الجانبي:** يقع دائمًا على يمين صفحة PDF، بغض النظر عن لغة الواجهة أو اتجاه النص.
  - **الهامش السفلي:** يقع أسفل صفحة PDF.
- يظهر في الهامشين نمط سطور كتابة بصري. يجب ألّا تمنع هذه السطور القراءة أو تحديد النص أو تحريره.
- يطبّق اتجاه الكتابة المناسب للمحتوى العربي افتراضيًا، مع بقاء النص المختلط (العربي/اللاتيني) قابلًا للتحرير.

### 6.5 تحرير وتنسيق الملاحظات

- يدعم كل هامش نصًا حرًا متعدد الأسطر.
- توفر شريط الأدوات أوامر: **غامق**، **قائمة نقطية**، **قائمة مرقمة**، وحجم خط **صغير/عادي/كبير**.
- تعمل الأوامر على التحديد الحالي، أو على موضع المؤشر عند عدم وجود تحديد، وفق سلوك محررات الويب المألوف.
- تقتصر البيانات المحفوظة على النص والعناصر الآتية فقط: `p` و`br` و`strong` و`b` و`ul` و`ol` و`li` و`span[data-font-size]`.
- يجب تعقيم المحتوى عند الحفظ والفتح والعرض؛ لا تُحفظ السمات أو السكربتات أو العناصر غير المسموح بها.

### 6.6 مقياس الهوامش

- يوفر عنصر تحكم واحدًا بثلاث قيم: `1` و`2` و`3` (تظهر للمستخدم بصيغة ×1، ×2، ×3).
- القيمة الافتراضية هي ×1.
- يغيّر المقياس مساحة الهامش الجانبي والسفلي معًا، وحجم الخط داخلهما، على جميع الصفحات دفعة واحدة.
- المقياس خاص بالمذكرة وليس بالصفحة؛ ولا يغيّر أبعاد أو محتوى PDF الأصلي.

### 6.7 الحفظ والتنزيل

- يوفر التطبيق زر «حفظ» متاحًا من مساحة العمل.
- عند الضغط عليه، يجمع التطبيق نسخة PDF الأصلية وجميع الملاحظات والإعدادات في ملف JSON بامتداد `.hamsh` ثم ينزّله مباشرة.
- يُحدّث `updatedAt` وآخر صفحة نشطة وقت الحفظ.
- يقترح اسم الملف `[اسم-PDF-الأصلي].hamsh` بعد إزالة امتداد `.pdf`، مع إتاحة أن يغيّر المتصفح الاسم أو الموقع حسب إعدادات المستخدم.
- الحفظ يدوي فقط؛ لا توجد آلية حفظ تلقائي أو تخزين محلي مستمر.

## 7. تدفق الاستخدام الأساسي

1. يفتح المستخدم التطبيق ويختار «مذكرة جديدة» أو «فتح مذكرة».
2. في المذكرة الجديدة، يختار ملف PDF صالحًا؛ وفي المذكرة الموجودة، يختار ملف `.hamsh` صالحًا.
3. ينتقل المستخدم إلى مساحة العمل ويكتب ملاحظاته في هامشي الصفحة، ويستخدم أدوات التنسيق عند الحاجة.
4. ينتقل بين الصفحات ويضبط مقياس الهوامش عند الحاجة.
5. يضغط «حفظ» لتنزيل ملف `.hamsh` محدّث.

## 8. نموذج البيانات وصيغة `.hamsh`

ملف `.hamsh` ملف UTF-8 بصيغة JSON وامتداد مخصص. لا يعتمد على خدمات خارجية.

```json
{
  "formatVersion": "1.0",
  "app": "hamesh",
  "meta": {
    "title": "اسم المذكرة",
    "originalFileName": "research-paper.pdf",
    "createdAt": "2026-08-18T10:00:00.000Z",
    "updatedAt": "2026-08-18T11:30:00.000Z"
  },
  "marginScale": 1,
  "viewState": {
    "activePage": 1
  },
  "pdf": {
    "data": "Base64-encoded PDF bytes",
    "pageCount": 12
  },
  "pages": [
    {
      "pageNumber": 1,
      "sideMargin": "<p><strong>ملاحظة</strong></p>",
      "bottomMargin": "<ul><li>نقطة أولى</li></ul>"
    }
  ]
}
```

### قواعد الصحة والتوافق

- `formatVersion` يجب أن يساوي `1.0` في هذا الإصدار.
- `app` يجب أن يساوي `hamesh`.
- `marginScale` قيمة صحيحة ضمن `{1, 2, 3}`.
- `pdf.data` يجب أن يكون Base64 صالحًا لملف PDF قابل للعرض.
- `pdf.pageCount` يجب أن يطابق عدد صفحات PDF الفعلي.
- تحتوي `pages` على عنصر واحد لكل صفحة، بأرقام متسلسلة تبدأ من 1.
- `viewState.activePage` عدد صحيح ضمن نطاق الصفحات؛ وإن غاب يُستخدم الرقم 1.
- عند فتح ملف أقدم يفتقد حقلاً اختياريًا، تُطبّق القيمة الافتراضية الآمنة حيث أمكن. أما المخالفة البنيوية أو عدم التوافق فيجب أن تمنع الفتح برسالة واضحة.

## 9. المتطلبات غير الوظيفية

### الخصوصية والأمان

- تعمل معالجة الملفات والعرض والتحرير والتصدير محليًا في المتصفح فقط.
- لا تُرسل ملفات PDF أو محتوى الملاحظات أو بيانات الاستخدام إلى خادم.
- يُعقّم HTML الملاحظات باستخدام قائمة سماح محددة قبل إدراجه في واجهة المستخدم أو تصديره.

### الأداء

- يُعرض مؤشر تحميل أثناء تحليل PDF أو تحميل صفحة جديدة.
- يجب أن تبقى واجهة التحرير مستجيبة عند التنقل بين الصفحات في المستندات الاعتيادية.
- يُعرض خطأ مفهوم إذا تعذّرت المعالجة بسبب حدود ذاكرة المتصفح أو ملف كبير جدًا؛ لا يوجد حد حجمي اصطناعي في المنتج ما لم تفرضه بيئة التشغيل.

### التوافق وإمكانية الوصول

- يستهدف التطبيق أحدث إصدارين من Chrome وEdge وFirefox وSafari على سطح المكتب.
- كل العناصر التفاعلية قابلة للوصول بلوحة المفاتيح، ولها أسماء مرئية أو تسميات مناسبة لقارئات الشاشة.
- لا تعتمد الوظائف الأساسية على السحب أو المؤشرات الدقيقة فقط.

## 10. حالات الخطأ الرئيسية

| الحالة | السلوك المطلوب |
| --- | --- |
| اختيار ملف ليس PDF عند إنشاء مذكرة | رفض الملف وعرض رسالة تطلب اختيار PDF صالح. |
| PDF تالف أو محمي بكلمة مرور | إبلاغ المستخدم بأن الملف لا يمكن فتحه في الإصدار الحالي. |
| ملف `.hamsh` ليس JSON صالحًا | رفض الملف وشرح أنه معطوب أو غير مدعوم. |
| إصدار صيغة غير مدعوم | رفض الفتح مع ذكر إصدار الملف والإصدارات المدعومة. |
| تعذّر تنزيل الملف | عرض رسالة إرشادية؛ تبقى المذكرة المفتوحة في الذاكرة دون فقدان فوري للمحتوى. |

## 11. معايير القبول

- يمكن للمستخدم رفع PDF صالح والانتقال مباشرةً إلى صفحة العمل مع العدد الصحيح للصفحات.
- يمكنه كتابة ملاحظات مختلفة في الهامشين لصفحتين أو أكثر، وتبقى كل ملاحظة مرتبطة بصفحتها بعد التنقل.
- تظهر أدوات الغامق والقائمة النقطية والقائمة المرقمة، وتُحفظ نتائجها وتُستعاد دون فقدان.
- يؤدي تغيير المقياس إلى تغيير الهامشين وحجم خطهما في كل الصفحات، ويُستعاد هذا الإعداد بعد فتح الملف المُصدّر.
- ينشئ الحفظ ملف `.hamsh` واحدًا يحتوي PDF والملاحظات، ويمكن فتحه لاحقًا لاستعادة المحتوى وفتح آخر صفحة محفوظة.
- لا تستخدم النسخة أي تخزين دائم داخل المتصفح ولا أي خدمة خلفية.
- يُرفض الملف غير الصالح دون انهيار الواجهة أو استبدال حالة مذكرة مفتوحة.

## 12. الاعتماديات والقيود التقنية

- استخدام **PDF.js** لقراءة وعرض ملفات PDF.
- استخدام واجهات المتصفح القياسية لقراءة الملفات وتنزيلها (`File`, `Blob`, و`URL.createObjectURL`).
- حجم ملف `.hamsh` سيكون أكبر من PDF الأصلي بسبب ترميز Base64؛ هذه مقايضة مقصودة لضمان ملف واحد مستقل في الإصدار الأول.

## 13. أسئلة مؤجّلة لما بعد MVP

- هل ينبغي دعم تصدير نسخة PDF قابلة للطباعة تتضمن الملاحظات؟
- هل تحتاج الملاحظات إلى بحث نصي أو فهرس؟
- هل سيكون التشفير المحلي لملفات `.hamsh` مطلوبًا لحالات الاستخدام الحساسة؟
