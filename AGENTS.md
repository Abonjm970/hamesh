# إرشادات مشروع هامش (Hamesh)

هذا الملف هو المرجع المختصر لأي تطوير لاحق في المشروع. اقرأه مع [PRD.md](PRD.md) قبل تعديل السلوك أو بنية البيانات.

## تعريف المشروع

«هامش» تطبيق ويب عربي محلي بالكامل لقراءة PDF وكتابة ملاحظات مرتبطة بكل صفحة. لا يوجد خادم أو حسابات أو حفظ تلقائي؛ ملف `.hamsh` هو صيغة الحفظ والنقل الوحيدة.

## بنية المشروع

- `index.html`: هيكل الواجهات، الصفحة الرئيسية ومساحة العمل.
- `styles.css`: نظام الألوان والتخطيط والاستجابة.
- `app.js`: إدارة الحالة، PDF.js، المحررات، التعقيم، الاستيراد والتصدير.
- `favicon.svg`: أيقونة التطبيق.
- `PRD.md`: المتطلبات الوظيفية ونطاق الإصدار.

لا توجد حاليًا حزمة بناء أو مدير اعتماديات؛ التطبيق يعمل كملفات ثابتة.

## تشغيل محلي

```bash
python3 -m http.server 4173
```

ثم افتح `http://localhost:4173/`. يلزم خادم HTTP محلي لأن التطبيق يستخدم ES Modules وPDF.js.

## قواعد التطوير

- حافظ على التطبيق client-side بالكامل. لا تضف API أو خادمًا أو تخزينًا دائمًا إلا بقرار موثق في `PRD.md`.
- استخدم JavaScript وCSS قياسيين، وفضّل الحل البسيط المتوافق مع البنية الحالية قبل إدخال إطار عمل.
- حافظ على اتجاه الواجهة `RTL`، وعلى الهامش الجانبي في يمين PDF دائمًا؛ استخدم `direction: ltr` لتخطيط صفحة القراءة عند الحاجة، لا لتغيير اتجاه النص.
- أي تنسيق جديد للملاحظات يجب أن يكون قابلًا للتعقيم والاستعادة. لا تسمح بـ`style` أو `script` أو سمات HTML عشوائية.
- عند تغيير صيغة `.hamsh` حدّث `formatVersion` وقواعد التحقق ونموذج البيانات في `PRD.md`، ووفّر توافقًا رجعيًا حيث يمكن.
- لا تستخدم `localStorage` أو `IndexedDB`. الحفظ يتم فقط عند ضغط المستخدم على «حفظ».
- استخدم النصوص العربية الواضحة في الواجهة، وتسميات وصول (`aria-label`) لكل عنصر تفاعلي غير بديهي.
- احترم `prefers-reduced-motion`، وحافظ على حالات التركيز المرئية والاستجابة للشاشات الصغيرة.

## الحالة وصيغة البيانات

الحالة الرئيسية في `app.js` هي: `pdfBytes`, `pdfDocument`, `pageCount`, `pages`, `marginScale`, `activePage`, و`meta`.

كل عنصر في `pages` مستقل عن غيره:

```js
{ pageNumber, sideMargin, bottomMargin }
```

الملاحظات HTML مقيدة بالعناصر `p`, `br`, `strong`, `b`, `ul`, `ol`, `li` و`span[data-font-size]` فقط. يجب تمرير المحتوى عبر `cleanHtml` عند الحفظ والفتح والعرض.

## الاختبار قبل التسليم

نفّذ فحص الصياغة:

```bash
node --check app.js
```

ثم اختبر يدويًا في Chrome DevTools أو متصفح حديث:

1. إنشاء مذكرة من PDF متعدد الصفحات.
2. الكتابة في الهامشين، بما في ذلك سطرين أو قوائم وتنسيقات مختلفة.
3. التنقل ذهابًا وإيابًا والتأكد من عدم فقدان المحتوى أو دمج الأسطر.
4. تغيير حجم الهامش وحجم الخط ثم الحفظ وإعادة الفتح.
5. اختبار ملف غير صالح، وأزرار الصفحة الأولى والأخيرة، وعرض الهاتف.
6. مراجعة Console والتأكد من عدم وجود أخطاء.

## اتجاه التصميم

التجربة تشبه ورقة بحثية على مكتب: خلفية حجرية هادئة، ورق أبيض دافئ، لون مرجاني للإشارات، وخطوط عربية `Noto Kufi Arabic` للواجهة و`Noto Naskh Arabic` للقراءة. أضف الزخرفة فقط إذا خَدَمت القراءة أو وضوح الحالة.

## تعريف الإنجاز

أي تغيير مكتمل يجب أن يحقق المتطلب في `PRD.md`، يعمل دون أخطاء في Console، لا يفقد ملاحظات الصفحات عند التنقل أو الحفظ، ويحافظ على الخصوصية وإمكانية الوصول والتصميم المتجاوب.
